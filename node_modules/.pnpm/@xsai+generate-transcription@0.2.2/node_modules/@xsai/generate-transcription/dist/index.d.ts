import { CommonRequestOptions } from '@xsai/shared';

interface GenerateTranscriptionOptions extends CommonRequestOptions {
    file: Blob;
    fileName?: string;
    language?: string;
    prompt?: string;
    temperature?: string;
}
interface GenerateTranscriptionResult {
    text: string;
}
declare const generateTranscription: (options: GenerateTranscriptionOptions) => Promise<GenerateTranscriptionResult>;

export { type GenerateTranscriptionOptions, type GenerateTranscriptionResult, generateTranscription };
