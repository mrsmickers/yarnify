import { CommonRequestOptions } from '@xsai/shared';

interface GenerateSpeechOptions extends CommonRequestOptions {
    [key: string]: unknown;
    input: string;
    /** @default `mp3` */
    responseFormat?: 'aac' | 'flac' | 'mp3' | 'opus' | 'pcm' | 'wav';
    /** @default `1.0` */
    speed?: number;
    voice: string;
}
declare const generateSpeech: (options: GenerateSpeechOptions) => Promise<ArrayBuffer>;

export { type GenerateSpeechOptions, generateSpeech };
